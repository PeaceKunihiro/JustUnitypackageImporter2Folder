# 更新履歴

## Version 0.1

### 2026-08-22

#### Importボタンの配置と状態表示

- 「これでインポートする」を「参照」「クリア」の右隣へ移動
- Import可能状態ではボタンを青色で表示
- 必要な入力が揃っていない場合はボタンを無効化

#### Import先プリセット

- 複数のImport先プリセットを保存できる機能を追加
- プリセットの表示名とインポート先パスを保持
- プルダウン選択時にImport先へ即時反映
- プリセットの追加、編集、削除、上下移動に対応
- プリセット数の固定上限を設けず、プロジェクト単位で永続化
- Default Import先とは別管理

#### 長いファイルパスへの対応

- GNU LongLink、POSIX PAX、USTARによる長いTARエントリ名の解析に対応
- 読み込んだ長いパスを切り詰めず、指定Import先へ展開
- OSまたはUnityの許容範囲を超えるパスをImport前に警告またはエラー表示

#### 展開後サイズと圧縮倍率

- UnityPackage解析時に展開後サイズと圧縮倍率を表示
- `asset`、`asset.meta`、`pathname`、`preview.png`およびその他の通常ファイルを集計対象に追加
- 展開後サイズが警告値以上の場合、赤色テキストで個別に警告
- 圧縮倍率が警告値以上の場合、赤色テキストで個別に警告
- 両方が警告値以上の場合、それぞれの警告を表示
- JUI Settingsへ展開後サイズ警告値を追加（初期値512 MB）
- JUI Settingsへ展開倍率警告値を追加（初期値10倍）
- 警告値をプロジェクト単位で永続化

#### 解析中の展開後サイズ確認

- 各TARエントリのヘッダー情報から、ペイロード読込前に展開後サイズを累積
- 累積展開後サイズが警告値を初めて超えた時点で解析を一時停止
- 「読み込みを続行」「キャンセル」の確認ダイアログを追加
- キャンセル時は超過エントリを読み込まず、入力と解析途中の内容をクリア

### 2026-08-21

#### 21:10

- UnityPackageのD&D領域を通常時は暗色、ドラッグ中は明色で表示
- UnityPackage読込時のGUID競合検査を追加
- GUID競合ファイルを薄い黄色でハイライト
- GUID競合ファイルのImportチェックを自動的に解除
- UnityPackage入力の「クリア」ボタンを追加

#### 20:33

- UnityPackageのトップレベル項目が2つ以上の場合、専用フォルダへまとめるか確認する機能を追加
- 専用フォルダを現在のImport先直下へ1階層追加
- 専用フォルダ名の初期値をUnityPackageの拡張子を除いたファイル名に設定
- Import前に専用フォルダ名を変更できるUIを追加
- 専用フォルダ名が10文字以上の場合の注意表示を追加

#### 19:36

- Import内容一覧をツリー形式・罫線表示へ変更
- バックアップを作成せずにImportできる選択肢を追加

#### 19:19

- Import完了後にUnityPackage入力欄をクリアするよう変更
- 重複時に「バックアップしないでインポートする」を追加
- 「バックアップフォルダを開く」ボタンを追加
- 「バックアップから復元する」ボタンを追加
- 復元前の「本当に復元しますか？」確認を追加
- JUIによる操作と警告をUnity Consoleへ出力
- 重複ファイル警告ダイアログを件数表示に変更し、詳細をConsoleへ出力

#### その他の修正

- Default Import先と通常Import通知設定を`EditorPrefs`からプロジェクト単位の`EditorUserSettings`へ変更
- UnityPackage読込時に不正な`pathname`を検出し、`Assets`外を指すPackageを拒否
- Import計画、競合検査、上書き検査、実書き込みの各段階で`Assets`外のパスを拒否
- Import処理へ内部トランザクションを追加
  - 上書き前のファイルを一時退避
  - 失敗時に既存ファイルを復元
  - 失敗時に新規作成ファイルと空フォルダを削除
  - 成功時に一時データを削除
- 未使用の`ToggleEntry`を削除
- ツリー選択状態の集計処理を軽量化
- READMEとSpecificationの設定保存方式を更新
